{
  "name": "futureproofing-eval-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute eval-engine-db --local --file=src/db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute eval-engine-db --remote --file=src/db/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
